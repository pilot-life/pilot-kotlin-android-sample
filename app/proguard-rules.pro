# Test app proguard rules. Intentionally minimal — the SDK and UI library
# ship their own consumer rules (META-INF/proguard/pilot-partner-sdk.pro
# and pilot-partner-ui/consumer-rules.pro), so nothing belongs here.
